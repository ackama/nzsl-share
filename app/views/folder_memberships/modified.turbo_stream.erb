<% sign = present(@membership.sign) %>
<%= turbo_stream.replace sign.dom_id(:folders), partial: "signs/card/folders", locals: { sign: sign, menuShown: true } %>
<%= turbo_stream.update_all ".count[data-folder-id='#{@membership.folder.id}']", "(#{@membership.folder.signs_count})" %>

