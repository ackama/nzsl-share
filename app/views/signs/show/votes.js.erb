<% sign = present(@sign) %>

<%# Re-render the folders menu for this sign %>
$("<%= escape_javascript(render 'signs/show/votes', sign: sign) %>")
  .replaceAll("#<%= sign.dom_id(:votes) %>")
  .foundation();
