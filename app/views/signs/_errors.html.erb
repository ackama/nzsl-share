<div id="sign-upload-errors" class="cell">
  <% if @sign.errors.any? %>
    <div class="callout alert">
      <% if @sign.errors.include?(:video) %>
        <h6>There were some problems with your sign video upload:</h6>
      <% end %>

      <ul>
        <%= content_tag(:li, @sign.errors[:conditions_accepted]) if @sign.errors[:conditions_accepted] %>
        <% @sign.errors.except(:conditions_accepted).full_messages.map do |err| %>
          <%= content_tag(:li, err) %>
        <% end %>
      </ul>

      <% if @sign.errors.include?(:video) %>
        Please check that your video complies with all the requirements laid out in
        our <a href="#sign-upload-tips">tips</a>.
      <% end %>
    </div>
  <% end %>
</div>
