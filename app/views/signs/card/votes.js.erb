<% sign = present(@sign) %>

<%# Re-render the voting controls for this sign %>
$("<%= escape_javascript(render 'signs/card/votes', sign: sign) %>")
  .replaceAll("#<%= sign.dom_id(:votes) %>")
  .foundation();
