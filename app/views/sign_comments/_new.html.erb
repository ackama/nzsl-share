<%= form_with(model: SignComment.new, url: sign_comment_index_path(sign), method: :post, remote: true) do |form| %>
  <%= form.text_field :comment, required: true, class: "sign-comments__contributor__box" %>
<% end %>
